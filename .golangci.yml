# golangci-lint configuration
# https://golangci-lint.run/usage/configuration/
version: "2"

run:
  timeout: 5m

issues:
  # Exclude macOS-only menubar app (requires systray which needs GTK on Linux)
  exclude-dirs:
    - cmd/cassh-menubar

linters:
  default: standard
  enable:
    - errcheck
